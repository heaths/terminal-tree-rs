[package]
name = "terminal_tree"
version = "0.1.0"
edition = "2021"
authors = [
    "Heath Stewart (https://github.com/heaths)"
]
repository = "https://github.com/heaths/terminal-tree-rs"
license = "MIT"

[dependencies]
crossterm = "0.27.0"
