{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "example: tree",
      "cargo": {
        "args": [
          "build",
          "--example=tree",
          "--features=clap"
        ]
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
